{
  "schema_version": 2,
  "role": "worker",
  "objective": "Make one precise correction to the in-progress v2.1.0 merge in /Users/berke/Downloads/Projects/chat-on-steroids. Read AGENTS.md and inspect the current staged merge first. Remove only the worker-introduced synchronous spawn wrapper from src/main/exec.ts: upstream v2.1.0 did not change this file, and the pre-merge fork ff5d1fa already had the intended child.on('error') handling plus the custom macOS PATH behavior. Restore src/main/exec.ts exactly to the ff5d1fa version unless the current file differs from ff5d1fa in another pre-existing custom hunk; the goal is that `git diff --cached ff5d1fa -- src/main/exec.ts` is empty. Do not alter the merge's upstream changes or any other production file. Keep the test/control-service.test.ts `await expect(...)` correction because it strengthens the existing custom socket-mode assertion without changing product behavior. Preserve MERGE_HEAD, all staged merge resolutions, generated notices, and all untracked .supervisor-shunt artifacts. Do not commit, push, install, publish, run nested agents, Goal, or Loop. After the edit, stage src/main/exec.ts, run git diff --cached --check, confirm src/main/exec.ts equals ff5d1fa, and run the direct TypeScript binary plus the smallest relevant direct Vitest command if the runner permits. Return exact changes and verification only.",
  "cwd": "/Users/berke/Downloads/Projects/chat-on-steroids",
  "files": [
    "src/main/exec.ts"
  ],
  "ownership": {
    "write": [
      "src/main/exec.ts"
    ],
    "read_extra": [
      "AGENTS.md",
      "test/control-service.test.ts",
      "package.json"
    ]
  },
  "verified_interfaces": [
    "pre-merge exec process behavior",
    "reviewable semantic merge"
  ],
  "constraints": [
    "Modify and stage only src/main/exec.ts.",
    "Do not change test/control-service.test.ts; its awaited assertion is intentionally retained.",
    "Do not change MERGE_HEAD or create a commit.",
    "No nested Shunt agents or model substitution."
  ],
  "verification": [
    [
      "git",
      "diff",
      "--cached",
      "--check"
    ],
    [
      "git",
      "diff",
      "--exit-code",
      "ff5d1fa",
      "--",
      "src/main/exec.ts"
    ]
  ],
  "previous_failure": {
    "error": "Supervisor review found one worker-only production workaround in src/main/exec.ts that was not part of either merge parent and was added solely for the worker sandbox."
  },
  "return_contract": {
    "summary": true,
    "files_changed": true,
    "verification": true,
    "unexpected_findings": true,
    "needs_supervisor_decision": true
  }
}
